# App Platform templates (not used)

Production is a **single VM / Droplet** running `server.js`. See [docs/DEPLOYMENT.md](../docs/DEPLOYMENT.md).

Do not deploy this folder with DigitalOcean App Platform `next start`. Queues and `/queues` would not run correctly.

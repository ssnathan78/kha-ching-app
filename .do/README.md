# App Platform (not used)

Production is a DigitalOcean **Droplet**. See `docs/DEPLOYMENT.md`.

This folder is leftover template only. Do not deploy with `next start` — the app must run `server.js` so queues and Bull Board work.

Repo: `ssnathan78/kha-ching-app`.
